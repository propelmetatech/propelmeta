// Database functionality disabled - using in-memory storage instead
// This file is kept for compatibility but exports are no longer used

console.log("Database disabled - using in-memory storage");

// Export dummy objects to prevent import errors
export const pool = null;
export const db = null;
